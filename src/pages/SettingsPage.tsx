import { SectionPlaceholder } from "../components/ui/SectionPlaceholder";

export function SettingsPage() {
  return (
    <SectionPlaceholder
      title="Settings, Security & Integrations"
      description="Manage profile, billing, API keys, permissions, team roles, and system-wide notification preferences in one premium control center."
    />
  );
}
